services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: eduapp_app
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    depends_on:
      - db
      - redis

  worker:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: eduapp_worker
    env_file:
      - .env
    environment:
      PROJECT_ROOT: ${PWD}
    volumes:
      - .:/app
      - ${PWD}:${PWD}
      - /var/run/docker.sock:/var/run/docker.sock
    command: rq worker ${REDIS_QUEUE_NAME:-submission_queue} --url ${REDIS_URL}
    depends_on:
      - db
      - redis

  db:
    image: postgres:16-alpine
    container_name: eduapp_db
    env_file:
      - .env
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    container_name: eduapp_redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

volumes:
  postgres_data:
  redis_data:
